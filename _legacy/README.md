# Legacy / Quarantined

Code in this directory is not used by the active system. It is kept for
historical reference only and may be deleted at any time.

- `src/` — older parallel implementation (pre-MCP rewrite)
- `mcp_server/` — abandoned single-server MCP prototype
- `list_transitions.py`, `reset_state.py` — scripts that imported from `src/`
- `*.md` — outdated documentation referring to the old `src/` layout

The active code lives in `agent/`, `mcp_servers/`, `scheduler/`,
`notifications/`, `observability/`, and `config/` at the repo root.
