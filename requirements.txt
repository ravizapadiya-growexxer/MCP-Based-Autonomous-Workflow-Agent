# Anthropic SDK (tool-use loop)
anthropic>=0.39.0

# HTTP client (Jira, Twilio)
httpx>=0.27.0

# Browser automation
playwright>=1.40.0

# Scheduler + timezone
apscheduler>=3.10.0
pytz>=2024.1

# Async SQLite state store
aiosqlite>=0.19.0

# Status server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Reliability helpers
tenacity>=8.2.0

# Settings + .env
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# India holiday calendar
holidays>=0.46

# Retained for future external MCP exposure (not used in core path)
mcp>=1.0.0
